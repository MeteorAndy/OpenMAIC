---
status: accepted
---

# Keep Diagnostics Local and User Controlled

Desktop Edition writes size-bounded local logs that redact Provider Credentials, request bodies, and user content, and it sends no automatic telemetry or crash reports. A Diagnostic Bundle is created only on explicit user action and excludes sensitive content by default, accepting less automatic support visibility to preserve the privacy expected of a local desktop product.
