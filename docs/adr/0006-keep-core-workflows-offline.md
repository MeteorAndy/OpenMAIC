---
status: accepted
---

# Keep Core Desktop Workflows Offline

Desktop Edition starts and supports browsing, editing, playback, backup, and export of existing Workspace Data during an Offline Session, while provider-backed generation and search report that network access is unavailable. Core UI assets are bundled instead of loaded from CDNs, accepting a larger installer so network failure cannot make the local product unusable or visually incomplete.
