---
status: accepted
---

# Run a Single Desktop Instance

Desktop Edition runs as one Desktop Instance so every launch uses the same local origin and Workspace Data remains reachable. A second launch activates the existing window; an unrelated process occupying the required local endpoint produces a visible, actionable error instead of silently exiting or switching to a new origin.
